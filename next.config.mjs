/** @type {import('next').NextConfig} */

const nextConfig = {
  // distDir: 'build',
  // reactStrictMode: true,
  // basePath: '/Projet_securit%C3%A9_infra_RISK_HORIZON/Projet_Infrastructure_IT_V2',
  // assetPrefix: '/Projet_securit%C3%A9_infra_RISK_HORIZON/Projet_Infrastructure_IT_V2/',
  // webpack5: true,
  // future: {
  //   webpack5: true,
  // },
  // webpack: (config, { isServer }) => {
  //   if (!isServer) {
  //     config.node = {
  //       fs: 'empty',
  //     };
  //   }
  //   return config;
  // },
  // exportPathMap: async function (
  //   defaultPathMap,
  //   { dev, dir, outDir, distDir, buildId }
  // ) {
  //   return {
  //     '/': { page: '/' },
  //     '/dashboard': { page: '/dashboard' } },
  // output: 'standalone',
};

export default nextConfig;
